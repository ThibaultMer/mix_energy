version: 1

sources:
  - name: nat_source
    database: mix-energie-gcp
    schema: "{{ target.schema }}"
    tables:
      - name: eco2mix_national_cons_def
      - name: eco2mix_national_tr

  - name: reg_source
    database: mix-energie-gcp
    schema: "{{ target.schema }}"
    tables:
      - name: eco2mix_regional_cons_def
      - name: eco2mix_regional_tr
